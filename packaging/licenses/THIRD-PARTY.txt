NeoST — licences of everything inside this package
==================================================

NeoST itself
------------
NeoST is free software under the **GNU General Public License version 3 or later**
(c) 2026 VERHILLE Arnaud. The full text is in `GPL-3.0.txt`, next to this file.

Complete corresponding source code for the binary in this package:

    https://github.com/habib256/neost

Hardware behaviour is largely ported from Hatari (GPLv2+), to which NeoST owes a great
deal; GPLv3 is compatible with that port.

Bundled components
------------------
| Component                         | Role                     | Licence                 |
|-----------------------------------|--------------------------|-------------------------|
| EmuTOS (`roms/etos*.img`)         | free TOS, the default    | **GPLv2** — `GPL-2.0.txt` |
| Moira (vendored, in the binary)   | cycle-exact 68000 core   | MIT — (c) Dirk W. Hoffmann |
| Dear ImGui (in the binary)        | interface                | MIT                     |
| miniaudio (in the binary)         | audio output             | MIT-0 / public domain   |
| GLFW 3.x (in the binary)          | window, input, GL context| zlib/libpng             |
| libmt32emu 2.8.3 (Munt, in the binary, **static**) | Roland MT-32 / CM-32L on MIDI OUT | **LGPL 2.1 or later** |
| stb_image v2.30 (in the binary)   | decodes the keyboard photo | MIT **or** public domain (Unlicense) |
| SDL2 2.30.9 (`lib/*/libSDL2.so`, **Android package only**) | window, GLES, audio, life cycle | zlib |
| DejaVu Sans (`fonts/`)            | UI font                  | DejaVu / Bitstream Vera free licence |
| Font Awesome Free (`fonts/`)      | UI icons                 | CC BY 4.0 + SIL OFL 1.1 |

GLFW is statically linked on macOS (built from the 3.4 tag) and on Windows (MinGW-w64);
the Linux AppImage carries `libglfw3.so` next to the binary, together with the system
libraries it pulls in, each under its own licence. Sources: https://www.glfw.org/

SDL2 replaces GLFW on Android only: https://libsdl.org/ (release-2.30.9).

Optional at build time and **not present in any package**: libslirp >= 4.7
(BSD-3-Clause, https://gitlab.freedesktop.org/slirp/libslirp), the user-mode NAT behind
the NE2000's real Internet access. The release builds are made without it.

EmuTOS source: https://emutos.sourceforge.io/ — the exact version shipped here is the
one named in the ROM's own banner (Options ▸ ROM in the interface, or `--version`).
Its sources are available from that site; NeoST redistributes the unmodified binary
images only.

Atari TOS images
----------------
⚠ `roms/tos102uk.img` and `roms/tos162uk.img`, when present in this package, are
**original Atari TOS ROMs**. They are NOT free software and NOT covered by the GPL:
they remain the property of their rights holders and are included only for the ST /
STE machine profiles. NeoST does not need them — it boots EmuTOS out of the box.

To build a package without them, set `NEOST_PACKAGE_NO_ATARI_TOS=1` in the environment
before running `packaging/stage_free_data.sh` (see that script's header).

Reference emulators
-------------------
Hatari (GPLv2+, https://framagit.org/hatari/hatari) and MAME are used as behavioural
*references* — read as sources, and run as an oracle during development. No part of
either is redistributed in this package.

libmt32emu and the LGPL
-----------------------
The Munt MT-32/CM-32L emulator (https://github.com/munt/munt, version 2.8.3) is vendored
under `extern/mt32emu` and linked **statically** into the GUI, which renders the ST's MIDI
OUT through a Roland MT-32/CM-32L. Upstream code is UNMODIFIED; only the CMake build file
is NeoST's (see `extern/mt32emu/NEOST_VENDOR.md`). LGPL 2.1 allows static linking as long
as the recipient can rebuild against a modified library: NeoST ships its full source under
GPL 3, this copy of Munt included, which satisfies that. Its section 3 also permits the
switch to the GPL, which settles compatibility with NeoST's GPL 3.
The Roland ROMs are NOT included: the user provides them (`roms/mt32/`).

stb_image (`extern/stb/stb_image.h`) is stb_image v2.30 by Sean Barrett and contributors,
dual-licensed MIT or public domain (Unlicense): https://github.com/nothings/stb — the GUI
uses it to decode the keyboard photo.
