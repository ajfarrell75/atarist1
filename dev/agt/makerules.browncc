# =============================================================================
#	[m68k-atari*-elf]
# =============================================================================

# =============================================================================
#	Generate .TOS file from linked .ELF
# =============================================================================

# =============================================================================
#	Rule build a single program (demo!)
# =============================================================================
$(OBJ_DIR)/%.elf: $(CRTSEQ_G_OBJ) $(CRTSEQ_S_OBJ) $(AGLIB_OBJ) $(OBJ_DIR)/%.o $(PROJECT_OBJ) $(CRTSEQ_E_OBJ) | Makefile
	$(LINK) -flto $(LDFLAGS) $(LIBPATHS) \
	$(CRTSEQ_G_OBJ) $(CRTSEQ_S_OBJ) $(OBJ_DIR)/$*.o $(PROJECT_OBJ) $(AGLIB_OBJ) $(LIBS) $(CRTSEQ_E_OBJ) \
	-o $(OBJ_DIR)/$*.elf

# -----------------------------------------------------------------------------
#	Convert ELF to TOS
# -----------------------------------------------------------------------------
$(OBJ_DIR)/%.tos: $(OBJ_DIR)/%.elf
# -----------------------------------------------------------------------------
ifeq ($(BUILD_CFG),release)
# -----------------------------------------------------------------------------
#	Convert executable
# -----------------------------------------------------------------------------
	$(HOST_TOOL_DIR)/brownout -p 0 -i $(OBJ_DIR)/$*.elf -o $(OBJ_DIR)/$*.tos
# -----------------------------------------------------------------------------
#	Strip symbols & pack executable
# -----------------------------------------------------------------------------
ifeq ($(USE_UPX_PACKER),yes)
	$(HOST_TOOL_DIR)/upx -9 $(OBJ_DIR)/$*.tos
#	$(HOST_TOOL_DIR)/upx -9 --brute --ultra-brute $(OBJ_DIR)/$*.tos
endif
# -----------------------------------------------------------------------------
else
# -----------------------------------------------------------------------------
#	Convert executable
# -----------------------------------------------------------------------------
	$(OBJDUMP) -h $(OBJ_DIR)/$*.elf
	$(HOST_TOOL_DIR)/brownout -s -x -p 0 -i $(OBJ_DIR)/$*.elf -o $(OBJ_DIR)/$*.tos
# -----------------------------------------------------------------------------
endif


